---
title: 客堂管理系统项目级开发约束
doc_type: agent-instructions
status: active
applies_to: ["cursor", "github-copilot", "kimi-cli"]
---

# 客堂管理系统 — 项目级开发约束

## 权威范围

- **[AGENTS.md](AGENTS.md)** 是跨工具通用基线（Karpathy 4 rules）。
- **本文**是项目级详细约束，与 `AGENTS.md` 互补。
- 若摘要与本文不一致，以本文为准。

## 0. 高优先级硬规则

1. **轻量文件组织**：核心入口为 `index.html`，样式在 `styles.css`，功能按模块放在 `js/*.js`，通过普通 `<script src>` 顺序加载；不引入 `src/features/` 或 ES Module 应用构建体系。
2. **不引入运行时构建链**：发布产物不得依赖 npm、Node、Webpack、Vite、React、Vue；允许开发者使用 npm devDependencies 做 lint/test/format/CI，只要不改变用户运行方式。
3. **数据不出院**：不默认使用公有云；本地 SQLite + IndexedDB。
4. **无商店认证**：不上架 App Store / 微软商店；不用 Electron / Tauri 安装包。
5. **`index.html` 复杂度阈值**：超过 2500 行时，同一任务必须提出拆分方案；拆分时保持非模块化 `<script src>`，确保 `file://` 可直接打开。
6. **所有用户输入必须转义**：通过 `escapeHtml()` 后再进入 `innerHTML`。
7. **未经实际验证不得完成**：每个改动必须附带验证证据。

## 一、核心原则

### 1. simplicity first

- 只解决用户提出的问题。
- 不为单一用例制造抽象。
- 200 行能写完就别写 500 行。

### 2. surgical changes

- 只碰必须改的文件和函数。
- 不「顺手改进」无关代码。
- 遵循现有代码风格（命名、缩进、注释双语）。

### 3. goal-driven execution

- 每个任务定义可验证的成功标准。
- 持久化改动必须：写入 → 刷新/重开 → 读回。
- 结束会话前说明验证了什么、怎么验证的。

## 二、数据模型与迁移

- 通过 `schema_version` 表管理版本。
- 新增字段/表必须写 migration，保证旧 `.db` 可恢复。
- 日期统一用 `YYYY-MM-DD` 字符串。

## 三、UI/UX 纪律

- 用户是寺院客堂非 IT 人员：大按钮、低学习成本。
- 用户可见文案用中文，保持寺院语义（挂单、退房、用斋、房态）。
- 关键操作后必须 Toast 提示。
- 删除、退房等不可逆操作必须二次确认。

## 四、新任务默认工作流

### 4.1 Explore（仅读不改）

只读：

- `index.html`、`styles.css`
- `js/` 下与本次改动相关的模块（通常先看 `app.js`、`db.js`，再定位具体功能文件）
- `README.md`、`使用说明.txt`、`AGENTS.md`

产出 ≤ 15 行「已读事实」：

1. 当前受影响的函数/视图。
2. 最相似的现有模式。
3. 与本次改动冲突的硬约束。
4. 已确认假设 vs 待澄清问题。

### 4.2 Research（新功能 / 新交互 / 新存储时）

调研维度：

1. 同类产品 / 标杆实现（寺院信息化如慧云台 / 通用 PMS）。
2. 业内 best practice / 事实标准。
3. 公认不可行 / 反模式。
4. 潜在坑（边界条件、兼容性、维护成本）。
5. 复用 / 适配 / 自研结论。

### 4.3 Plan（落位 + 验证方式，等用户确认）

Plan 必含：

1. 成熟方案结论（复用/适配/自研 + 理由）。
2. 新增逻辑落位：哪个函数 / 哪个视图。
3. 验证方式：Chrome Headless 命令 + 手动业务路径。
4. 是否触及 schema 迁移。

### 4.4 Implement（执行 Plan，逐步验证）

- 严格按 Plan 落位。
- 不顺手改无关代码。
- 每完成一个功能点就验证渲染/路径。

### 4.5 Verify（结束前的强制步骤）

1. 启动本地服务：`python3 -m http.server 8080`
2. Chrome Headless 渲染无错误。
3. 手动跑通至少一条业务路径。
4. 更新 `README.md` / `使用说明.txt`（如功能有变）。

## 五、禁止模式

1. 拆分出的 JS 模块未按依赖顺序在 `index.html` 中引入，或引入 ES Module 破坏 `file://` 兼容性。
2. 引入与当前运行架构不一致的目录体系或构建链（如 Vite/Webpack/React/Vue，或要求用户安装 Node/npm）。
3. 把简单功能过度抽象成多层函数。
4. 未经验证就声称完成。
5. 用户输入直接拼进 `innerHTML`。

## 六、提交前自检

1. 新增业务逻辑是否还能再简化？
2. 是否所有用户输入都转义了？
3. 是否触发了 2500 行阈值？
4. 是否补了验证证据？
5. 是否更新了文档？

如果答案不理想，继续重构，而不是直接停止。
